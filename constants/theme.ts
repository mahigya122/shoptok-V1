import { colors } from './colors';

// Minimal theme mapping used by the app. Adjust as needed.
export const Colors = {
  light: {
    background: colors.background,
    tint: colors.primary,
    inactive: colors.textMuted,
    backgroundAlt: colors.surface
  },
  dark: {
    background: colors.background,
    tint: colors.primary,
    inactive: colors.textMuted,
    backgroundAlt: colors.surface
  }
};

export default Colors;
